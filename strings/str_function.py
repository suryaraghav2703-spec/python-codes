name = "surya"
print(len(name))#len function gives length of the string
print(name.endswith("rya"))
print(name.startswith("su"))#they are case sensitive so be carefull about space and capital
print(name.capitalize())#only capitalize first word

print(name)# Strings are immutable which means that you cannot change them by running functions on them